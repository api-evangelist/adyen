{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/PinChangeRequest",
  "title": "PinChangeRequest",
  "properties": {
    "encryptedKey": {
      "description": "Symmetric session key encrypted under the public key.",
      "type": "string"
    },
    "encryptedPinBlock": {
      "description": "The encrypted PIN block",
      "type": "string"
    },
    "paymentInstrumentId": {
      "description": "The unique identifier of the payment instrument.",
      "type": "string"
    },
    "token": {
      "description": "The token which is used to construct the pinblock.",
      "type": "string"
    }
  },
  "required": [
    "paymentInstrumentId",
    "encryptedKey",
    "token",
    "encryptedPinBlock"
  ],
  "type": "object"
}