{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/DeleteBankAccountRequest",
  "title": "DeleteBankAccountRequest",
  "properties": {
    "accountHolderCode": {
      "description": "The code of the Account Holder from which to delete the Bank Account(s).",
      "type": "string"
    },
    "bankAccountUUIDs": {
      "description": "The code(s) of the Bank Accounts to be deleted.",
      "items": {
        "type": "string"
      },
      "type": "array"
    }
  },
  "required": [
    "accountHolderCode",
    "bankAccountUUIDs"
  ],
  "type": "object"
}