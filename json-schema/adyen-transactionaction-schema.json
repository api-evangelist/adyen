{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "#/components/schemas/TransactionAction",
  "title": "TransactionAction",
  "type": "string",
  "description": "Action to realise on a transaction.",
  "enum": [
    "AbortTransaction",
    "StartTransaction"
  ]
}