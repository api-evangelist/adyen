naftiko: 1.0.0-alpha2
info:
  label: Adyen Disputes API — deleteDisputeDefenseDocument
  description: 'Adyen Disputes API — deleteDisputeDefenseDocument. 1 operations. Lead operation: Adyen Delete a Defense Document.
    Self-contained Naftiko capability covering one Adyen business surface.'
  tags:
  - Adyen
  - deleteDisputeDefenseDocument
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ADYEN_API_KEY: ADYEN_API_KEY
capability:
  consumes:
  - type: http
    namespace: disputes-deletedisputedefensedocument
    baseUri: https://ca-test.adyen.com/ca/services/DisputeService/v30
    description: Adyen Disputes API — deleteDisputeDefenseDocument business capability. Self-contained, no shared references.
    resources:
    - name: deleteDisputeDefenseDocument
      path: /deleteDisputeDefenseDocument
      operations:
      - name: postdeletedisputedefensedocument
        method: POST
        description: Adyen Delete a Defense Document
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: X-API-Key
      value: '{{env.ADYEN_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: disputes-deletedisputedefensedocument-rest
    port: 8080
    description: REST adapter for Adyen Disputes API — deleteDisputeDefenseDocument. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/deletedisputedefensedocument
      name: deletedisputedefensedocument
      description: REST surface for deleteDisputeDefenseDocument.
      operations:
      - method: POST
        name: postdeletedisputedefensedocument
        description: Adyen Delete a Defense Document
        call: disputes-deletedisputedefensedocument.postdeletedisputedefensedocument
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: disputes-deletedisputedefensedocument-mcp
    port: 9090
    transport: http
    description: MCP adapter for Adyen Disputes API — deleteDisputeDefenseDocument. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: adyen-delete-defense-document
      description: Adyen Delete a Defense Document
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: disputes-deletedisputedefensedocument.postdeletedisputedefensedocument
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
