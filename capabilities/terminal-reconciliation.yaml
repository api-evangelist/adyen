naftiko: 1.0.0-alpha2
info:
  label: Adyen Terminal API — Reconciliation
  description: 'Adyen Terminal API — Reconciliation. 1 operations. Lead operation: Adyen Reconciliation Request. Self-contained
    Naftiko capability covering one Adyen business surface.'
  tags:
  - Adyen
  - Reconciliation
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ADYEN_API_KEY: ADYEN_API_KEY
capability:
  consumes:
  - type: http
    namespace: terminal-reconciliation
    baseUri: https://terminal-api-test.adyen.com/sync
    description: Adyen Terminal API — Reconciliation business capability. Self-contained, no shared references.
    resources:
    - name: reconciliation
      path: /reconciliation
      operations:
      - name: post
        method: POST
        description: Adyen Reconciliation Request
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: X-API-Key
      value: '{{env.ADYEN_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: terminal-reconciliation-rest
    port: 8080
    description: REST adapter for Adyen Terminal API — Reconciliation. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/reconciliation
      name: reconciliation
      description: REST surface for reconciliation.
      operations:
      - method: POST
        name: post
        description: Adyen Reconciliation Request
        call: terminal-reconciliation.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: terminal-reconciliation-mcp
    port: 9090
    transport: http
    description: MCP adapter for Adyen Terminal API — Reconciliation. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: adyen-reconciliation-request
      description: Adyen Reconciliation Request
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: terminal-reconciliation.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
