naftiko: 1.0.0-alpha2
info:
  label: Adyen Terminal API — Storedvalue
  description: 'Adyen Terminal API — Storedvalue. 1 operations. Lead operation: Adyen StoredValue Request. Self-contained
    Naftiko capability covering one Adyen business surface.'
  tags:
  - Adyen
  - Storedvalue
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ADYEN_API_KEY: ADYEN_API_KEY
capability:
  consumes:
  - type: http
    namespace: terminal-storedvalue
    baseUri: https://terminal-api-test.adyen.com/sync
    description: Adyen Terminal API — Storedvalue business capability. Self-contained, no shared references.
    resources:
    - name: storedvalue
      path: /storedvalue
      operations:
      - name: post
        method: POST
        description: Adyen StoredValue Request
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: X-API-Key
      value: '{{env.ADYEN_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: terminal-storedvalue-rest
    port: 8080
    description: REST adapter for Adyen Terminal API — Storedvalue. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/storedvalue
      name: storedvalue
      description: REST surface for storedvalue.
      operations:
      - method: POST
        name: post
        description: Adyen StoredValue Request
        call: terminal-storedvalue.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: terminal-storedvalue-mcp
    port: 9090
    transport: http
    description: MCP adapter for Adyen Terminal API — Storedvalue. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: adyen-storedvalue-request
      description: Adyen StoredValue Request
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: terminal-storedvalue.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
