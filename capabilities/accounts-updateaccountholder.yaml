naftiko: 1.0.0-alpha2
info:
  label: Adyen Account API — updateAccountHolder
  description: 'Adyen Account API — updateAccountHolder. 1 operations. Lead operation: Adyen Update an Account Holder. Self-contained
    Naftiko capability covering one Adyen business surface.'
  tags:
  - Adyen
  - updateAccountHolder
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ADYEN_API_KEY: ADYEN_API_KEY
capability:
  consumes:
  - type: http
    namespace: accounts-updateaccountholder
    baseUri: https://cal-test.adyen.com/cal/services/Account/v6
    description: Adyen Account API — updateAccountHolder business capability. Self-contained, no shared references.
    resources:
    - name: updateAccountHolder
      path: /updateAccountHolder
      operations:
      - name: postupdateaccountholder
        method: POST
        description: Adyen Update an Account Holder
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: X-API-Key
      value: '{{env.ADYEN_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: accounts-updateaccountholder-rest
    port: 8080
    description: REST adapter for Adyen Account API — updateAccountHolder. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/updateaccountholder
      name: updateaccountholder
      description: REST surface for updateAccountHolder.
      operations:
      - method: POST
        name: postupdateaccountholder
        description: Adyen Update an Account Holder
        call: accounts-updateaccountholder.postupdateaccountholder
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: accounts-updateaccountholder-mcp
    port: 9090
    transport: http
    description: MCP adapter for Adyen Account API — updateAccountHolder. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: adyen-update-account-holder
      description: Adyen Update an Account Holder
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: accounts-updateaccountholder.postupdateaccountholder
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
