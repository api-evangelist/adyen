naftiko: 1.0.0-alpha2
info:
  label: Adyen Notification Configuration API — testNotificationConfiguration
  description: 'Adyen Notification Configuration API — testNotificationConfiguration. 1 operations. Lead operation: Adyen
    Test a Notification Configuration. Self-contained Naftiko capability covering one Adyen business surface.'
  tags:
  - Adyen
  - testNotificationConfiguration
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ADYEN_API_KEY: ADYEN_API_KEY
capability:
  consumes:
  - type: http
    namespace: notification-configurations-testnotificationconfiguration
    baseUri: https://cal-test.adyen.com/cal/services/Notification/v6
    description: Adyen Notification Configuration API — testNotificationConfiguration business capability. Self-contained,
      no shared references.
    resources:
    - name: testNotificationConfiguration
      path: /testNotificationConfiguration
      operations:
      - name: posttestnotificationconfiguration
        method: POST
        description: Adyen Test a Notification Configuration
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: X-API-Key
      value: '{{env.ADYEN_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: notification-configurations-testnotificationconfiguration-rest
    port: 8080
    description: REST adapter for Adyen Notification Configuration API — testNotificationConfiguration. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/testnotificationconfiguration
      name: testnotificationconfiguration
      description: REST surface for testNotificationConfiguration.
      operations:
      - method: POST
        name: posttestnotificationconfiguration
        description: Adyen Test a Notification Configuration
        call: notification-configurations-testnotificationconfiguration.posttestnotificationconfiguration
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: notification-configurations-testnotificationconfiguration-mcp
    port: 9090
    transport: http
    description: MCP adapter for Adyen Notification Configuration API — testNotificationConfiguration. One tool per consumed
      operation, routed inline through this capability's consumes block.
    tools:
    - name: adyen-test-notification-configuration
      description: Adyen Test a Notification Configuration
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: notification-configurations-testnotificationconfiguration.posttestnotificationconfiguration
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
