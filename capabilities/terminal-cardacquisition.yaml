naftiko: 1.0.0-alpha2
info:
  label: Adyen Terminal API — Cardacquisition
  description: 'Adyen Terminal API — Cardacquisition. 1 operations. Lead operation: Adyen CardAcquisition Request. Self-contained
    Naftiko capability covering one Adyen business surface.'
  tags:
  - Adyen
  - Cardacquisition
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ADYEN_API_KEY: ADYEN_API_KEY
capability:
  consumes:
  - type: http
    namespace: terminal-cardacquisition
    baseUri: https://terminal-api-test.adyen.com/sync
    description: Adyen Terminal API — Cardacquisition business capability. Self-contained, no shared references.
    resources:
    - name: cardacquisition
      path: /cardacquisition
      operations:
      - name: post
        method: POST
        description: Adyen CardAcquisition Request
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: X-API-Key
      value: '{{env.ADYEN_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: terminal-cardacquisition-rest
    port: 8080
    description: REST adapter for Adyen Terminal API — Cardacquisition. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/cardacquisition
      name: cardacquisition
      description: REST surface for cardacquisition.
      operations:
      - method: POST
        name: post
        description: Adyen CardAcquisition Request
        call: terminal-cardacquisition.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: terminal-cardacquisition-mcp
    port: 9090
    transport: http
    description: MCP adapter for Adyen Terminal API — Cardacquisition. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: adyen-cardacquisition-request
      description: Adyen CardAcquisition Request
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: terminal-cardacquisition.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
