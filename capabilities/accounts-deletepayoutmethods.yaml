naftiko: 1.0.0-alpha2
info:
  label: Adyen Account API — deletePayoutMethods
  description: 'Adyen Account API — deletePayoutMethods. 1 operations. Lead operation: Adyen Delete Payout Methods. Self-contained
    Naftiko capability covering one Adyen business surface.'
  tags:
  - Adyen
  - deletePayoutMethods
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ADYEN_API_KEY: ADYEN_API_KEY
capability:
  consumes:
  - type: http
    namespace: accounts-deletepayoutmethods
    baseUri: https://cal-test.adyen.com/cal/services/Account/v6
    description: Adyen Account API — deletePayoutMethods business capability. Self-contained, no shared references.
    resources:
    - name: deletePayoutMethods
      path: /deletePayoutMethods
      operations:
      - name: postdeletepayoutmethods
        method: POST
        description: Adyen Delete Payout Methods
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: X-API-Key
      value: '{{env.ADYEN_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: accounts-deletepayoutmethods-rest
    port: 8080
    description: REST adapter for Adyen Account API — deletePayoutMethods. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/deletepayoutmethods
      name: deletepayoutmethods
      description: REST surface for deletePayoutMethods.
      operations:
      - method: POST
        name: postdeletepayoutmethods
        description: Adyen Delete Payout Methods
        call: accounts-deletepayoutmethods.postdeletepayoutmethods
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: accounts-deletepayoutmethods-mcp
    port: 9090
    transport: http
    description: MCP adapter for Adyen Account API — deletePayoutMethods. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: adyen-delete-payout-methods
      description: Adyen Delete Payout Methods
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: accounts-deletepayoutmethods.postdeletepayoutmethods
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
