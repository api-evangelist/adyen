naftiko: 1.0.0-alpha2
info:
  label: Adyen Terminal API — Balanceinquiry
  description: 'Adyen Terminal API — Balanceinquiry. 1 operations. Lead operation: Adyen BalanceInquiry Request. Self-contained
    Naftiko capability covering one Adyen business surface.'
  tags:
  - Adyen
  - Balanceinquiry
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ADYEN_API_KEY: ADYEN_API_KEY
capability:
  consumes:
  - type: http
    namespace: terminal-balanceinquiry
    baseUri: https://terminal-api-test.adyen.com/sync
    description: Adyen Terminal API — Balanceinquiry business capability. Self-contained, no shared references.
    resources:
    - name: balanceinquiry
      path: /balanceinquiry
      operations:
      - name: post
        method: POST
        description: Adyen BalanceInquiry Request
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: X-API-Key
      value: '{{env.ADYEN_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: terminal-balanceinquiry-rest
    port: 8080
    description: REST adapter for Adyen Terminal API — Balanceinquiry. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/balanceinquiry
      name: balanceinquiry
      description: REST surface for balanceinquiry.
      operations:
      - method: POST
        name: post
        description: Adyen BalanceInquiry Request
        call: terminal-balanceinquiry.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: terminal-balanceinquiry-mcp
    port: 9090
    transport: http
    description: MCP adapter for Adyen Terminal API — Balanceinquiry. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: adyen-balanceinquiry-request
      description: Adyen BalanceInquiry Request
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: terminal-balanceinquiry.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
