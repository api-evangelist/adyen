naftiko: 1.0.0-alpha2
info:
  label: Adyen Recurring API — createPermit
  description: 'Adyen Recurring API — createPermit. 1 operations. Lead operation: Adyen Create New Permits Linked to a Recurring
    Contract.. Self-contained Naftiko capability covering one Adyen business surface.'
  tags:
  - Adyen
  - createPermit
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ADYEN_API_KEY: ADYEN_API_KEY
capability:
  consumes:
  - type: http
    namespace: recurring-createpermit
    baseUri: https://pal-test.adyen.com/pal/servlet/Recurring/v68
    description: Adyen Recurring API — createPermit business capability. Self-contained, no shared references.
    resources:
    - name: createPermit
      path: /createPermit
      operations:
      - name: postcreatepermit
        method: POST
        description: Adyen Create New Permits Linked to a Recurring Contract.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: X-API-Key
      value: '{{env.ADYEN_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: recurring-createpermit-rest
    port: 8080
    description: REST adapter for Adyen Recurring API — createPermit. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/createpermit
      name: createpermit
      description: REST surface for createPermit.
      operations:
      - method: POST
        name: postcreatepermit
        description: Adyen Create New Permits Linked to a Recurring Contract.
        call: recurring-createpermit.postcreatepermit
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: recurring-createpermit-mcp
    port: 9090
    transport: http
    description: MCP adapter for Adyen Recurring API — createPermit. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: adyen-create-new-permits-linked
      description: Adyen Create New Permits Linked to a Recurring Contract.
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: recurring-createpermit.postcreatepermit
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
