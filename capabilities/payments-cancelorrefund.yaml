naftiko: 1.0.0-alpha2
info:
  label: Adyen Payment API — cancelOrRefund
  description: 'Adyen Payment API — cancelOrRefund. 1 operations. Lead operation: Adyen Cancel or Refund a Payment. Self-contained
    Naftiko capability covering one Adyen business surface.'
  tags:
  - Adyen
  - cancelOrRefund
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ADYEN_API_KEY: ADYEN_API_KEY
capability:
  consumes:
  - type: http
    namespace: payments-cancelorrefund
    baseUri: https://pal-test.adyen.com/pal/servlet/Payment/v68
    description: Adyen Payment API — cancelOrRefund business capability. Self-contained, no shared references.
    resources:
    - name: cancelOrRefund
      path: /cancelOrRefund
      operations:
      - name: postcancelorrefund
        method: POST
        description: Adyen Cancel or Refund a Payment
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: X-API-Key
      value: '{{env.ADYEN_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: payments-cancelorrefund-rest
    port: 8080
    description: REST adapter for Adyen Payment API — cancelOrRefund. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/cancelorrefund
      name: cancelorrefund
      description: REST surface for cancelOrRefund.
      operations:
      - method: POST
        name: postcancelorrefund
        description: Adyen Cancel or Refund a Payment
        call: payments-cancelorrefund.postcancelorrefund
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: payments-cancelorrefund-mcp
    port: 9090
    transport: http
    description: MCP adapter for Adyen Payment API — cancelOrRefund. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: adyen-cancel-refund-payment
      description: Adyen Cancel or Refund a Payment
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: payments-cancelorrefund.postcancelorrefund
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
