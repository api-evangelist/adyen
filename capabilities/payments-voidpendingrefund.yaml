naftiko: 1.0.0-alpha2
info:
  label: Adyen Payment API — voidPendingRefund
  description: 'Adyen Payment API — voidPendingRefund. 1 operations. Lead operation: Adyen Cancel an In-person Refund. Self-contained
    Naftiko capability covering one Adyen business surface.'
  tags:
  - Adyen
  - voidPendingRefund
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ADYEN_API_KEY: ADYEN_API_KEY
capability:
  consumes:
  - type: http
    namespace: payments-voidpendingrefund
    baseUri: https://pal-test.adyen.com/pal/servlet/Payment/v68
    description: Adyen Payment API — voidPendingRefund business capability. Self-contained, no shared references.
    resources:
    - name: voidPendingRefund
      path: /voidPendingRefund
      operations:
      - name: postvoidpendingrefund
        method: POST
        description: Adyen Cancel an In-person Refund
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: X-API-Key
      value: '{{env.ADYEN_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: payments-voidpendingrefund-rest
    port: 8080
    description: REST adapter for Adyen Payment API — voidPendingRefund. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/voidpendingrefund
      name: voidpendingrefund
      description: REST surface for voidPendingRefund.
      operations:
      - method: POST
        name: postvoidpendingrefund
        description: Adyen Cancel an In-person Refund
        call: payments-voidpendingrefund.postvoidpendingrefund
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: payments-voidpendingrefund-mcp
    port: 9090
    transport: http
    description: MCP adapter for Adyen Payment API — voidPendingRefund. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: adyen-cancel-person-refund
      description: Adyen Cancel an In-person Refund
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: payments-voidpendingrefund.postvoidpendingrefund
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
