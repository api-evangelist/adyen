naftiko: 1.0.0-alpha2
info:
  label: Adyen Account API — closeAccountHolder
  description: 'Adyen Account API — closeAccountHolder. 1 operations. Lead operation: Adyen Close an Account Holder. Self-contained
    Naftiko capability covering one Adyen business surface.'
  tags:
  - Adyen
  - closeAccountHolder
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ADYEN_API_KEY: ADYEN_API_KEY
capability:
  consumes:
  - type: http
    namespace: accounts-closeaccountholder
    baseUri: https://cal-test.adyen.com/cal/services/Account/v6
    description: Adyen Account API — closeAccountHolder business capability. Self-contained, no shared references.
    resources:
    - name: closeAccountHolder
      path: /closeAccountHolder
      operations:
      - name: postcloseaccountholder
        method: POST
        description: Adyen Close an Account Holder
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: X-API-Key
      value: '{{env.ADYEN_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: accounts-closeaccountholder-rest
    port: 8080
    description: REST adapter for Adyen Account API — closeAccountHolder. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/closeaccountholder
      name: closeaccountholder
      description: REST surface for closeAccountHolder.
      operations:
      - method: POST
        name: postcloseaccountholder
        description: Adyen Close an Account Holder
        call: accounts-closeaccountholder.postcloseaccountholder
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: accounts-closeaccountholder-mcp
    port: 9090
    transport: http
    description: MCP adapter for Adyen Account API — closeAccountHolder. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: adyen-close-account-holder
      description: Adyen Close an Account Holder
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: accounts-closeaccountholder.postcloseaccountholder
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
