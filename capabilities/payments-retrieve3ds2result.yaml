naftiko: 1.0.0-alpha2
info:
  label: Adyen Payment API — retrieve3ds2Result
  description: 'Adyen Payment API — retrieve3ds2Result. 1 operations. Lead operation: Adyen Get the 3DS2 Authentication Result.
    Self-contained Naftiko capability covering one Adyen business surface.'
  tags:
  - Adyen
  - retrieve3ds2Result
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ADYEN_API_KEY: ADYEN_API_KEY
capability:
  consumes:
  - type: http
    namespace: payments-retrieve3ds2result
    baseUri: https://pal-test.adyen.com/pal/servlet/Payment/v68
    description: Adyen Payment API — retrieve3ds2Result business capability. Self-contained, no shared references.
    resources:
    - name: retrieve3ds2Result
      path: /retrieve3ds2Result
      operations:
      - name: postretrieve3ds2result
        method: POST
        description: Adyen Get the 3DS2 Authentication Result
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: X-API-Key
      value: '{{env.ADYEN_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: payments-retrieve3ds2result-rest
    port: 8080
    description: REST adapter for Adyen Payment API — retrieve3ds2Result. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/retrieve3ds2result
      name: retrieve3ds2result
      description: REST surface for retrieve3ds2Result.
      operations:
      - method: POST
        name: postretrieve3ds2result
        description: Adyen Get the 3DS2 Authentication Result
        call: payments-retrieve3ds2result.postretrieve3ds2result
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: payments-retrieve3ds2result-mcp
    port: 9090
    transport: http
    description: MCP adapter for Adyen Payment API — retrieve3ds2Result. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: adyen-get-3ds2-authentication-result
      description: Adyen Get the 3DS2 Authentication Result
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: payments-retrieve3ds2result.postretrieve3ds2result
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
