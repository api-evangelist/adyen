naftiko: 1.0.0-alpha2
info:
  label: Adyen Terminal API — Reversal
  description: 'Adyen Terminal API — Reversal. 1 operations. Lead operation: Adyen Reversal Request. Self-contained Naftiko
    capability covering one Adyen business surface.'
  tags:
  - Adyen
  - Reversal
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ADYEN_API_KEY: ADYEN_API_KEY
capability:
  consumes:
  - type: http
    namespace: terminal-reversal
    baseUri: https://terminal-api-test.adyen.com/sync
    description: Adyen Terminal API — Reversal business capability. Self-contained, no shared references.
    resources:
    - name: reversal
      path: /reversal
      operations:
      - name: post
        method: POST
        description: Adyen Reversal Request
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: X-API-Key
      value: '{{env.ADYEN_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: terminal-reversal-rest
    port: 8080
    description: REST adapter for Adyen Terminal API — Reversal. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/reversal
      name: reversal
      description: REST surface for reversal.
      operations:
      - method: POST
        name: post
        description: Adyen Reversal Request
        call: terminal-reversal.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: terminal-reversal-mcp
    port: 9090
    transport: http
    description: MCP adapter for Adyen Terminal API — Reversal. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: adyen-reversal-request
      description: Adyen Reversal Request
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: terminal-reversal.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
