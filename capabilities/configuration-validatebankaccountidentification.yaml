naftiko: 1.0.0-alpha2
info:
  label: Adyen Configuration API — validateBankAccountIdentification
  description: 'Adyen Configuration API — validateBankAccountIdentification. 1 operations. Lead operation: Adyen Validate
    a Bank Account. Self-contained Naftiko capability covering one Adyen business surface.'
  tags:
  - Adyen
  - validateBankAccountIdentification
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ADYEN_API_KEY: ADYEN_API_KEY
capability:
  consumes:
  - type: http
    namespace: configuration-validatebankaccountidentification
    baseUri: https://balanceplatform-api-test.adyen.com/bcl/v2
    description: Adyen Configuration API — validateBankAccountIdentification business capability. Self-contained, no shared
      references.
    resources:
    - name: validateBankAccountIdentification
      path: /validateBankAccountIdentification
      operations:
      - name: postvalidatebankaccountidentification
        method: POST
        description: Adyen Validate a Bank Account
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: X-API-Key
      value: '{{env.ADYEN_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: configuration-validatebankaccountidentification-rest
    port: 8080
    description: REST adapter for Adyen Configuration API — validateBankAccountIdentification. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/validatebankaccountidentification
      name: validatebankaccountidentification
      description: REST surface for validateBankAccountIdentification.
      operations:
      - method: POST
        name: postvalidatebankaccountidentification
        description: Adyen Validate a Bank Account
        call: configuration-validatebankaccountidentification.postvalidatebankaccountidentification
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: configuration-validatebankaccountidentification-mcp
    port: 9090
    transport: http
    description: MCP adapter for Adyen Configuration API — validateBankAccountIdentification. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: adyen-validate-bank-account
      description: Adyen Validate a Bank Account
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: configuration-validatebankaccountidentification.postvalidatebankaccountidentification
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
