naftiko: 1.0.0-alpha2
info:
  label: Adyen Account API — suspendAccountHolder
  description: 'Adyen Account API — suspendAccountHolder. 1 operations. Lead operation: Adyen Suspend an Account Holder. Self-contained
    Naftiko capability covering one Adyen business surface.'
  tags:
  - Adyen
  - suspendAccountHolder
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ADYEN_API_KEY: ADYEN_API_KEY
capability:
  consumes:
  - type: http
    namespace: accounts-suspendaccountholder
    baseUri: https://cal-test.adyen.com/cal/services/Account/v6
    description: Adyen Account API — suspendAccountHolder business capability. Self-contained, no shared references.
    resources:
    - name: suspendAccountHolder
      path: /suspendAccountHolder
      operations:
      - name: postsuspendaccountholder
        method: POST
        description: Adyen Suspend an Account Holder
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: X-API-Key
      value: '{{env.ADYEN_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: accounts-suspendaccountholder-rest
    port: 8080
    description: REST adapter for Adyen Account API — suspendAccountHolder. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/suspendaccountholder
      name: suspendaccountholder
      description: REST surface for suspendAccountHolder.
      operations:
      - method: POST
        name: postsuspendaccountholder
        description: Adyen Suspend an Account Holder
        call: accounts-suspendaccountholder.postsuspendaccountholder
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: accounts-suspendaccountholder-mcp
    port: 9090
    transport: http
    description: MCP adapter for Adyen Account API — suspendAccountHolder. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: adyen-suspend-account-holder
      description: Adyen Suspend an Account Holder
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: accounts-suspendaccountholder.postsuspendaccountholder
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
