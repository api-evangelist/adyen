naftiko: 1.0.0-alpha2
info:
  label: Adyen Disputes API — supplyDefenseDocument
  description: 'Adyen Disputes API — supplyDefenseDocument. 1 operations. Lead operation: Adyen Supply a Defense Document.
    Self-contained Naftiko capability covering one Adyen business surface.'
  tags:
  - Adyen
  - supplyDefenseDocument
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ADYEN_API_KEY: ADYEN_API_KEY
capability:
  consumes:
  - type: http
    namespace: disputes-supplydefensedocument
    baseUri: https://ca-test.adyen.com/ca/services/DisputeService/v30
    description: Adyen Disputes API — supplyDefenseDocument business capability. Self-contained, no shared references.
    resources:
    - name: supplyDefenseDocument
      path: /supplyDefenseDocument
      operations:
      - name: postsupplydefensedocument
        method: POST
        description: Adyen Supply a Defense Document
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: X-API-Key
      value: '{{env.ADYEN_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: disputes-supplydefensedocument-rest
    port: 8080
    description: REST adapter for Adyen Disputes API — supplyDefenseDocument. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/supplydefensedocument
      name: supplydefensedocument
      description: REST surface for supplyDefenseDocument.
      operations:
      - method: POST
        name: postsupplydefensedocument
        description: Adyen Supply a Defense Document
        call: disputes-supplydefensedocument.postsupplydefensedocument
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: disputes-supplydefensedocument-mcp
    port: 9090
    transport: http
    description: MCP adapter for Adyen Disputes API — supplyDefenseDocument. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: adyen-supply-defense-document
      description: Adyen Supply a Defense Document
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: disputes-supplydefensedocument.postsupplydefensedocument
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
