naftiko: 1.0.0-alpha2
info:
  label: Adyen Recurring API — scheduleAccountUpdater
  description: 'Adyen Recurring API — scheduleAccountUpdater. 1 operations. Lead operation: Adyen Schedule Running the Account
    Updater. Self-contained Naftiko capability covering one Adyen business surface.'
  tags:
  - Adyen
  - scheduleAccountUpdater
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ADYEN_API_KEY: ADYEN_API_KEY
capability:
  consumes:
  - type: http
    namespace: recurring-scheduleaccountupdater
    baseUri: https://pal-test.adyen.com/pal/servlet/Recurring/v68
    description: Adyen Recurring API — scheduleAccountUpdater business capability. Self-contained, no shared references.
    resources:
    - name: scheduleAccountUpdater
      path: /scheduleAccountUpdater
      operations:
      - name: postscheduleaccountupdater
        method: POST
        description: Adyen Schedule Running the Account Updater
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: X-API-Key
      value: '{{env.ADYEN_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: recurring-scheduleaccountupdater-rest
    port: 8080
    description: REST adapter for Adyen Recurring API — scheduleAccountUpdater. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/scheduleaccountupdater
      name: scheduleaccountupdater
      description: REST surface for scheduleAccountUpdater.
      operations:
      - method: POST
        name: postscheduleaccountupdater
        description: Adyen Schedule Running the Account Updater
        call: recurring-scheduleaccountupdater.postscheduleaccountupdater
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: recurring-scheduleaccountupdater-mcp
    port: 9090
    transport: http
    description: MCP adapter for Adyen Recurring API — scheduleAccountUpdater. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: adyen-schedule-running-account-updater
      description: Adyen Schedule Running the Account Updater
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: recurring-scheduleaccountupdater.postscheduleaccountupdater
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
