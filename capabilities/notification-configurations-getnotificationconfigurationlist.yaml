naftiko: 1.0.0-alpha2
info:
  label: Adyen Notification Configuration API — getNotificationConfigurationList
  description: 'Adyen Notification Configuration API — getNotificationConfigurationList. 1 operations. Lead operation: Adyen
    Get a List of Notification Subscription Configurations. Self-contained Naftiko capability covering one Adyen business
    surface.'
  tags:
  - Adyen
  - getNotificationConfigurationList
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ADYEN_API_KEY: ADYEN_API_KEY
capability:
  consumes:
  - type: http
    namespace: notification-configurations-getnotificationconfigurationlist
    baseUri: https://cal-test.adyen.com/cal/services/Notification/v6
    description: Adyen Notification Configuration API — getNotificationConfigurationList business capability. Self-contained,
      no shared references.
    resources:
    - name: getNotificationConfigurationList
      path: /getNotificationConfigurationList
      operations:
      - name: postgetnotificationconfigurationlist
        method: POST
        description: Adyen Get a List of Notification Subscription Configurations
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: X-API-Key
      value: '{{env.ADYEN_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: notification-configurations-getnotificationconfigurationlist-rest
    port: 8080
    description: REST adapter for Adyen Notification Configuration API — getNotificationConfigurationList. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/getnotificationconfigurationlist
      name: getnotificationconfigurationlist
      description: REST surface for getNotificationConfigurationList.
      operations:
      - method: POST
        name: postgetnotificationconfigurationlist
        description: Adyen Get a List of Notification Subscription Configurations
        call: notification-configurations-getnotificationconfigurationlist.postgetnotificationconfigurationlist
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: notification-configurations-getnotificationconfigurationlist-mcp
    port: 9090
    transport: http
    description: MCP adapter for Adyen Notification Configuration API — getNotificationConfigurationList. One tool per consumed
      operation, routed inline through this capability's consumes block.
    tools:
    - name: adyen-get-list-notification-subscription
      description: Adyen Get a List of Notification Subscription Configurations
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: notification-configurations-getnotificationconfigurationlist.postgetnotificationconfigurationlist
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
