naftiko: 1.0.0-alpha2
info:
  label: Adyen Terminal API — Login
  description: 'Adyen Terminal API — Login. 1 operations. Lead operation: Adyen Login Request. Self-contained Naftiko capability
    covering one Adyen business surface.'
  tags:
  - Adyen
  - Login
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ADYEN_API_KEY: ADYEN_API_KEY
capability:
  consumes:
  - type: http
    namespace: terminal-login
    baseUri: https://terminal-api-test.adyen.com/sync
    description: Adyen Terminal API — Login business capability. Self-contained, no shared references.
    resources:
    - name: login
      path: /login
      operations:
      - name: post
        method: POST
        description: Adyen Login Request
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: X-API-Key
      value: '{{env.ADYEN_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: terminal-login-rest
    port: 8080
    description: REST adapter for Adyen Terminal API — Login. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/login
      name: login
      description: REST surface for login.
      operations:
      - method: POST
        name: post
        description: Adyen Login Request
        call: terminal-login.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: terminal-login-mcp
    port: 9090
    transport: http
    description: MCP adapter for Adyen Terminal API — Login. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: adyen-login-request
      description: Adyen Login Request
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: terminal-login.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
