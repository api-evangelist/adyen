naftiko: 1.0.0-alpha2
info:
  label: Adyen Recurring API — disablePermit
  description: 'Adyen Recurring API — disablePermit. 1 operations. Lead operation: Adyen Disable an Existing Permit.. Self-contained
    Naftiko capability covering one Adyen business surface.'
  tags:
  - Adyen
  - disablePermit
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ADYEN_API_KEY: ADYEN_API_KEY
capability:
  consumes:
  - type: http
    namespace: recurring-disablepermit
    baseUri: https://pal-test.adyen.com/pal/servlet/Recurring/v68
    description: Adyen Recurring API — disablePermit business capability. Self-contained, no shared references.
    resources:
    - name: disablePermit
      path: /disablePermit
      operations:
      - name: postdisablepermit
        method: POST
        description: Adyen Disable an Existing Permit.
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: X-API-Key
      value: '{{env.ADYEN_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: recurring-disablepermit-rest
    port: 8080
    description: REST adapter for Adyen Recurring API — disablePermit. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/disablepermit
      name: disablepermit
      description: REST surface for disablePermit.
      operations:
      - method: POST
        name: postdisablepermit
        description: Adyen Disable an Existing Permit.
        call: recurring-disablepermit.postdisablepermit
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: recurring-disablepermit-mcp
    port: 9090
    transport: http
    description: MCP adapter for Adyen Recurring API — disablePermit. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: adyen-disable-existing-permit
      description: Adyen Disable an Existing Permit.
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: recurring-disablepermit.postdisablepermit
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
