naftiko: 1.0.0-alpha2
info:
  label: Adyen Payment API — technicalCancel
  description: 'Adyen Payment API — technicalCancel. 1 operations. Lead operation: Adyen Cancel an Authorisation Using Your
    Reference. Self-contained Naftiko capability covering one Adyen business surface.'
  tags:
  - Adyen
  - technicalCancel
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ADYEN_API_KEY: ADYEN_API_KEY
capability:
  consumes:
  - type: http
    namespace: payments-technicalcancel
    baseUri: https://pal-test.adyen.com/pal/servlet/Payment/v68
    description: Adyen Payment API — technicalCancel business capability. Self-contained, no shared references.
    resources:
    - name: technicalCancel
      path: /technicalCancel
      operations:
      - name: posttechnicalcancel
        method: POST
        description: Adyen Cancel an Authorisation Using Your Reference
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: X-API-Key
      value: '{{env.ADYEN_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: payments-technicalcancel-rest
    port: 8080
    description: REST adapter for Adyen Payment API — technicalCancel. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/technicalcancel
      name: technicalcancel
      description: REST surface for technicalCancel.
      operations:
      - method: POST
        name: posttechnicalcancel
        description: Adyen Cancel an Authorisation Using Your Reference
        call: payments-technicalcancel.posttechnicalcancel
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: payments-technicalcancel-mcp
    port: 9090
    transport: http
    description: MCP adapter for Adyen Payment API — technicalCancel. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: adyen-cancel-authorisation-using-your
      description: Adyen Cancel an Authorisation Using Your Reference
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: payments-technicalcancel.posttechnicalcancel
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
