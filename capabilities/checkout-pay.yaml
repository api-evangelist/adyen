naftiko: 1.0.0-alpha2
info:
  label: Adyen Checkout API — Pay
  description: 'Adyen Checkout API — Pay. 1 operations. Lead operation: Adyen Get an Apple Pay Session. Self-contained Naftiko
    capability covering one Adyen business surface.'
  tags:
  - Adyen
  - Pay
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ADYEN_API_KEY: ADYEN_API_KEY
capability:
  consumes:
  - type: http
    namespace: checkout-pay
    baseUri: https://checkout-test.adyen.com/v71
    description: Adyen Checkout API — Pay business capability. Self-contained, no shared references.
    resources:
    - name: applePay-sessions
      path: /applePay/sessions
      operations:
      - name: postapplepaysessions
        method: POST
        description: Adyen Get an Apple Pay Session
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: X-API-Key
      value: '{{env.ADYEN_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: checkout-pay-rest
    port: 8080
    description: REST adapter for Adyen Checkout API — Pay. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/applepay/sessions
      name: applepay-sessions
      description: REST surface for applePay-sessions.
      operations:
      - method: POST
        name: postapplepaysessions
        description: Adyen Get an Apple Pay Session
        call: checkout-pay.postapplepaysessions
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: checkout-pay-mcp
    port: 9090
    transport: http
    description: MCP adapter for Adyen Checkout API — Pay. One tool per consumed operation, routed inline through this capability's
      consumes block.
    tools:
    - name: adyen-get-apple-pay-session
      description: Adyen Get an Apple Pay Session
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: checkout-pay.postapplepaysessions
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
