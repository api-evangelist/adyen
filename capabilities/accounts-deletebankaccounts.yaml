naftiko: 1.0.0-alpha2
info:
  label: Adyen Account API — deleteBankAccounts
  description: 'Adyen Account API — deleteBankAccounts. 1 operations. Lead operation: Adyen Delete Bank Accounts. Self-contained
    Naftiko capability covering one Adyen business surface.'
  tags:
  - Adyen
  - deleteBankAccounts
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ADYEN_API_KEY: ADYEN_API_KEY
capability:
  consumes:
  - type: http
    namespace: accounts-deletebankaccounts
    baseUri: https://cal-test.adyen.com/cal/services/Account/v6
    description: Adyen Account API — deleteBankAccounts business capability. Self-contained, no shared references.
    resources:
    - name: deleteBankAccounts
      path: /deleteBankAccounts
      operations:
      - name: postdeletebankaccounts
        method: POST
        description: Adyen Delete Bank Accounts
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: X-API-Key
      value: '{{env.ADYEN_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: accounts-deletebankaccounts-rest
    port: 8080
    description: REST adapter for Adyen Account API — deleteBankAccounts. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/deletebankaccounts
      name: deletebankaccounts
      description: REST surface for deleteBankAccounts.
      operations:
      - method: POST
        name: postdeletebankaccounts
        description: Adyen Delete Bank Accounts
        call: accounts-deletebankaccounts.postdeletebankaccounts
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: accounts-deletebankaccounts-mcp
    port: 9090
    transport: http
    description: MCP adapter for Adyen Account API — deleteBankAccounts. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: adyen-delete-bank-accounts
      description: Adyen Delete Bank Accounts
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: accounts-deletebankaccounts.postdeletebankaccounts
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
