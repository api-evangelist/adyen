naftiko: 1.0.0-alpha2
info:
  label: Adyen Terminal API — Loyalty
  description: 'Adyen Terminal API — Loyalty. 1 operations. Lead operation: Adyen Loyalty Request. Self-contained Naftiko
    capability covering one Adyen business surface.'
  tags:
  - Adyen
  - Loyalty
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ADYEN_API_KEY: ADYEN_API_KEY
capability:
  consumes:
  - type: http
    namespace: terminal-loyalty
    baseUri: https://terminal-api-test.adyen.com/sync
    description: Adyen Terminal API — Loyalty business capability. Self-contained, no shared references.
    resources:
    - name: loyalty
      path: /loyalty
      operations:
      - name: post
        method: POST
        description: Adyen Loyalty Request
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: X-API-Key
      value: '{{env.ADYEN_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: terminal-loyalty-rest
    port: 8080
    description: REST adapter for Adyen Terminal API — Loyalty. One Spectral-compliant resource per consumed operation, prefixed
      with /v1.
    resources:
    - path: /v1/loyalty
      name: loyalty
      description: REST surface for loyalty.
      operations:
      - method: POST
        name: post
        description: Adyen Loyalty Request
        call: terminal-loyalty.post
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: terminal-loyalty-mcp
    port: 9090
    transport: http
    description: MCP adapter for Adyen Terminal API — Loyalty. One tool per consumed operation, routed inline through this
      capability's consumes block.
    tools:
    - name: adyen-loyalty-request
      description: Adyen Loyalty Request
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: terminal-loyalty.post
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
