naftiko: 1.0.0-alpha2
info:
  label: Adyen Notification Configuration API — getNotificationConfiguration
  description: 'Adyen Notification Configuration API — getNotificationConfiguration. 1 operations. Lead operation: Adyen Get
    a Notification Subscription Configuration. Self-contained Naftiko capability covering one Adyen business surface.'
  tags:
  - Adyen
  - getNotificationConfiguration
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ADYEN_API_KEY: ADYEN_API_KEY
capability:
  consumes:
  - type: http
    namespace: notification-configurations-getnotificationconfiguration
    baseUri: https://cal-test.adyen.com/cal/services/Notification/v6
    description: Adyen Notification Configuration API — getNotificationConfiguration business capability. Self-contained,
      no shared references.
    resources:
    - name: getNotificationConfiguration
      path: /getNotificationConfiguration
      operations:
      - name: postgetnotificationconfiguration
        method: POST
        description: Adyen Get a Notification Subscription Configuration
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: X-API-Key
      value: '{{env.ADYEN_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: notification-configurations-getnotificationconfiguration-rest
    port: 8080
    description: REST adapter for Adyen Notification Configuration API — getNotificationConfiguration. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/getnotificationconfiguration
      name: getnotificationconfiguration
      description: REST surface for getNotificationConfiguration.
      operations:
      - method: POST
        name: postgetnotificationconfiguration
        description: Adyen Get a Notification Subscription Configuration
        call: notification-configurations-getnotificationconfiguration.postgetnotificationconfiguration
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: notification-configurations-getnotificationconfiguration-mcp
    port: 9090
    transport: http
    description: MCP adapter for Adyen Notification Configuration API — getNotificationConfiguration. One tool per consumed
      operation, routed inline through this capability's consumes block.
    tools:
    - name: adyen-get-notification-subscription-configuration
      description: Adyen Get a Notification Subscription Configuration
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: notification-configurations-getnotificationconfiguration.postgetnotificationconfiguration
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
