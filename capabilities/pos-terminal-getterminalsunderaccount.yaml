naftiko: 1.0.0-alpha2
info:
  label: Adyen POS Terminal Management API — getTerminalsUnderAccount
  description: 'Adyen POS Terminal Management API — getTerminalsUnderAccount. 1 operations. Lead operation: Adyen Get the
    List of Terminals. Self-contained Naftiko capability covering one Adyen business surface.'
  tags:
  - Adyen
  - getTerminalsUnderAccount
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ADYEN_API_KEY: ADYEN_API_KEY
capability:
  consumes:
  - type: http
    namespace: pos-terminal-getterminalsunderaccount
    baseUri: https://postfmapi-test.adyen.com/postfmapi/terminal/v1
    description: Adyen POS Terminal Management API — getTerminalsUnderAccount business capability. Self-contained, no shared
      references.
    resources:
    - name: getTerminalsUnderAccount
      path: /getTerminalsUnderAccount
      operations:
      - name: postgetterminalsunderaccount
        method: POST
        description: Adyen Get the List of Terminals
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: X-API-Key
      value: '{{env.ADYEN_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: pos-terminal-getterminalsunderaccount-rest
    port: 8080
    description: REST adapter for Adyen POS Terminal Management API — getTerminalsUnderAccount. One Spectral-compliant resource
      per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/getterminalsunderaccount
      name: getterminalsunderaccount
      description: REST surface for getTerminalsUnderAccount.
      operations:
      - method: POST
        name: postgetterminalsunderaccount
        description: Adyen Get the List of Terminals
        call: pos-terminal-getterminalsunderaccount.postgetterminalsunderaccount
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: pos-terminal-getterminalsunderaccount-mcp
    port: 9090
    transport: http
    description: MCP adapter for Adyen POS Terminal Management API — getTerminalsUnderAccount. One tool per consumed operation,
      routed inline through this capability's consumes block.
    tools:
    - name: adyen-get-list-terminals
      description: Adyen Get the List of Terminals
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: pos-terminal-getterminalsunderaccount.postgetterminalsunderaccount
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
