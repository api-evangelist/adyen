naftiko: 1.0.0-alpha2
info:
  label: Adyen Account API — updateAccountHolderState
  description: 'Adyen Account API — updateAccountHolderState. 1 operations. Lead operation: Adyen Update Payout or Processing
    State. Self-contained Naftiko capability covering one Adyen business surface.'
  tags:
  - Adyen
  - updateAccountHolderState
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ADYEN_API_KEY: ADYEN_API_KEY
capability:
  consumes:
  - type: http
    namespace: accounts-updateaccountholderstate
    baseUri: https://cal-test.adyen.com/cal/services/Account/v6
    description: Adyen Account API — updateAccountHolderState business capability. Self-contained, no shared references.
    resources:
    - name: updateAccountHolderState
      path: /updateAccountHolderState
      operations:
      - name: postupdateaccountholderstate
        method: POST
        description: Adyen Update Payout or Processing State
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: X-API-Key
      value: '{{env.ADYEN_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: accounts-updateaccountholderstate-rest
    port: 8080
    description: REST adapter for Adyen Account API — updateAccountHolderState. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/updateaccountholderstate
      name: updateaccountholderstate
      description: REST surface for updateAccountHolderState.
      operations:
      - method: POST
        name: postupdateaccountholderstate
        description: Adyen Update Payout or Processing State
        call: accounts-updateaccountholderstate.postupdateaccountholderstate
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: accounts-updateaccountholderstate-mcp
    port: 9090
    transport: http
    description: MCP adapter for Adyen Account API — updateAccountHolderState. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: adyen-update-payout-processing-state
      description: Adyen Update Payout or Processing State
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: accounts-updateaccountholderstate.postupdateaccountholderstate
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
