naftiko: 1.0.0-alpha2
info:
  label: Adyen Notification Configuration API — updateNotificationConfiguration
  description: 'Adyen Notification Configuration API — updateNotificationConfiguration. 1 operations. Lead operation: Adyen
    Update a Notification Subscription Configuration. Self-contained Naftiko capability covering one Adyen business surface.'
  tags:
  - Adyen
  - updateNotificationConfiguration
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ADYEN_API_KEY: ADYEN_API_KEY
capability:
  consumes:
  - type: http
    namespace: notification-configurations-updatenotificationconfiguration
    baseUri: https://cal-test.adyen.com/cal/services/Notification/v6
    description: Adyen Notification Configuration API — updateNotificationConfiguration business capability. Self-contained,
      no shared references.
    resources:
    - name: updateNotificationConfiguration
      path: /updateNotificationConfiguration
      operations:
      - name: postupdatenotificationconfiguration
        method: POST
        description: Adyen Update a Notification Subscription Configuration
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: X-API-Key
      value: '{{env.ADYEN_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: notification-configurations-updatenotificationconfiguration-rest
    port: 8080
    description: REST adapter for Adyen Notification Configuration API — updateNotificationConfiguration. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/updatenotificationconfiguration
      name: updatenotificationconfiguration
      description: REST surface for updateNotificationConfiguration.
      operations:
      - method: POST
        name: postupdatenotificationconfiguration
        description: Adyen Update a Notification Subscription Configuration
        call: notification-configurations-updatenotificationconfiguration.postupdatenotificationconfiguration
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: notification-configurations-updatenotificationconfiguration-mcp
    port: 9090
    transport: http
    description: MCP adapter for Adyen Notification Configuration API — updateNotificationConfiguration. One tool per consumed
      operation, routed inline through this capability's consumes block.
    tools:
    - name: adyen-update-notification-subscription-configuration
      description: Adyen Update a Notification Subscription Configuration
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: notification-configurations-updatenotificationconfiguration.postupdatenotificationconfiguration
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
