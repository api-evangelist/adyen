naftiko: 1.0.0-alpha2
info:
  label: Adyen Notification Configuration API — createNotificationConfiguration
  description: 'Adyen Notification Configuration API — createNotificationConfiguration. 1 operations. Lead operation: Adyen
    Subscribe to Notifications. Self-contained Naftiko capability covering one Adyen business surface.'
  tags:
  - Adyen
  - createNotificationConfiguration
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ADYEN_API_KEY: ADYEN_API_KEY
capability:
  consumes:
  - type: http
    namespace: notification-configurations-createnotificationconfiguration
    baseUri: https://cal-test.adyen.com/cal/services/Notification/v6
    description: Adyen Notification Configuration API — createNotificationConfiguration business capability. Self-contained,
      no shared references.
    resources:
    - name: createNotificationConfiguration
      path: /createNotificationConfiguration
      operations:
      - name: postcreatenotificationconfiguration
        method: POST
        description: Adyen Subscribe to Notifications
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: X-API-Key
      value: '{{env.ADYEN_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: notification-configurations-createnotificationconfiguration-rest
    port: 8080
    description: REST adapter for Adyen Notification Configuration API — createNotificationConfiguration. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/createnotificationconfiguration
      name: createnotificationconfiguration
      description: REST surface for createNotificationConfiguration.
      operations:
      - method: POST
        name: postcreatenotificationconfiguration
        description: Adyen Subscribe to Notifications
        call: notification-configurations-createnotificationconfiguration.postcreatenotificationconfiguration
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: notification-configurations-createnotificationconfiguration-mcp
    port: 9090
    transport: http
    description: MCP adapter for Adyen Notification Configuration API — createNotificationConfiguration. One tool per consumed
      operation, routed inline through this capability's consumes block.
    tools:
    - name: adyen-subscribe-notifications
      description: Adyen Subscribe to Notifications
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: notification-configurations-createnotificationconfiguration.postcreatenotificationconfiguration
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
