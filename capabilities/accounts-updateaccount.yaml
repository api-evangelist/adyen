naftiko: 1.0.0-alpha2
info:
  label: Adyen Account API — updateAccount
  description: 'Adyen Account API — updateAccount. 1 operations. Lead operation: Adyen Update an Account. Self-contained Naftiko
    capability covering one Adyen business surface.'
  tags:
  - Adyen
  - updateAccount
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ADYEN_API_KEY: ADYEN_API_KEY
capability:
  consumes:
  - type: http
    namespace: accounts-updateaccount
    baseUri: https://cal-test.adyen.com/cal/services/Account/v6
    description: Adyen Account API — updateAccount business capability. Self-contained, no shared references.
    resources:
    - name: updateAccount
      path: /updateAccount
      operations:
      - name: postupdateaccount
        method: POST
        description: Adyen Update an Account
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: X-API-Key
      value: '{{env.ADYEN_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: accounts-updateaccount-rest
    port: 8080
    description: REST adapter for Adyen Account API — updateAccount. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/updateaccount
      name: updateaccount
      description: REST surface for updateAccount.
      operations:
      - method: POST
        name: postupdateaccount
        description: Adyen Update an Account
        call: accounts-updateaccount.postupdateaccount
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: accounts-updateaccount-mcp
    port: 9090
    transport: http
    description: MCP adapter for Adyen Account API — updateAccount. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: adyen-update-account
      description: Adyen Update an Account
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: accounts-updateaccount.postupdateaccount
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
