naftiko: 1.0.0-alpha2
info:
  label: Adyen Fund API — debitAccountHolder
  description: 'Adyen Fund API — debitAccountHolder. 1 operations. Lead operation: Adyen Send a Direct Debit Request. Self-contained
    Naftiko capability covering one Adyen business surface.'
  tags:
  - Adyen
  - debitAccountHolder
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ADYEN_API_KEY: ADYEN_API_KEY
capability:
  consumes:
  - type: http
    namespace: funds-debitaccountholder
    baseUri: https://cal-test.adyen.com/cal/services/Fund/v6
    description: Adyen Fund API — debitAccountHolder business capability. Self-contained, no shared references.
    resources:
    - name: debitAccountHolder
      path: /debitAccountHolder
      operations:
      - name: postdebitaccountholder
        method: POST
        description: Adyen Send a Direct Debit Request
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: X-API-Key
      value: '{{env.ADYEN_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: funds-debitaccountholder-rest
    port: 8080
    description: REST adapter for Adyen Fund API — debitAccountHolder. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/debitaccountholder
      name: debitaccountholder
      description: REST surface for debitAccountHolder.
      operations:
      - method: POST
        name: postdebitaccountholder
        description: Adyen Send a Direct Debit Request
        call: funds-debitaccountholder.postdebitaccountholder
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: funds-debitaccountholder-mcp
    port: 9090
    transport: http
    description: MCP adapter for Adyen Fund API — debitAccountHolder. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: adyen-send-direct-debit-request
      description: Adyen Send a Direct Debit Request
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: funds-debitaccountholder.postdebitaccountholder
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
