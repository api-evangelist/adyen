naftiko: 1.0.0-alpha2
info:
  label: Adyen Payment API — getAuthenticationResult
  description: 'Adyen Payment API — getAuthenticationResult. 1 operations. Lead operation: Adyen Get the 3DS Authentication
    Result. Self-contained Naftiko capability covering one Adyen business surface.'
  tags:
  - Adyen
  - getAuthenticationResult
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    ADYEN_API_KEY: ADYEN_API_KEY
capability:
  consumes:
  - type: http
    namespace: payments-getauthenticationresult
    baseUri: https://pal-test.adyen.com/pal/servlet/Payment/v68
    description: Adyen Payment API — getAuthenticationResult business capability. Self-contained, no shared references.
    resources:
    - name: getAuthenticationResult
      path: /getAuthenticationResult
      operations:
      - name: postgetauthenticationresult
        method: POST
        description: Adyen Get the 3DS Authentication Result
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: false
    authentication:
      type: apikey
      key: X-API-Key
      value: '{{env.ADYEN_API_KEY}}'
      placement: header
  exposes:
  - type: rest
    namespace: payments-getauthenticationresult-rest
    port: 8080
    description: REST adapter for Adyen Payment API — getAuthenticationResult. One Spectral-compliant resource per consumed
      operation, prefixed with /v1.
    resources:
    - path: /v1/getauthenticationresult
      name: getauthenticationresult
      description: REST surface for getAuthenticationResult.
      operations:
      - method: POST
        name: postgetauthenticationresult
        description: Adyen Get the 3DS Authentication Result
        call: payments-getauthenticationresult.postgetauthenticationresult
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: payments-getauthenticationresult-mcp
    port: 9090
    transport: http
    description: MCP adapter for Adyen Payment API — getAuthenticationResult. One tool per consumed operation, routed inline
      through this capability's consumes block.
    tools:
    - name: adyen-get-3ds-authentication-result
      description: Adyen Get the 3DS Authentication Result
      hints:
        readOnly: true
        destructive: false
        idempotent: false
      call: payments-getauthenticationresult.postgetauthenticationresult
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
