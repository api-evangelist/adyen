{
  "opencollection": "1.0.0",
  "info": {
    "name": "Adyen Account acceptDispute createNotificationConfiguration API",
    "version": "6"
  },
  "items": [
    {
      "info": {
        "name": "createNotificationConfiguration",
        "type": "folder"
      },
      "items": [
        {
          "info": {
            "name": "Adyen Subscribe to Notifications",
            "type": "http"
          },
          "http": {
            "method": "POST",
            "url": "https://cal-test.adyen.com/cal/services/Account/v6/createNotificationConfiguration",
            "body": {
              "type": "json",
              "data": "{}"
            },
            "auth": {
              "type": "basic",
              "username": "{{username}}",
              "password": "{{password}}"
            }
          },
          "docs": "Creates a subscription to notifications informing you of events on your platform. After the subscription is created, the events specified in the configuration will be sent to the URL specified in the configuration. Subscriptions must be configured on a per-event basis (as opposed to, for example, a per-account holder basis), so all event notifications of a marketplace and of a given type will be sent to the same endpoint(s). A marketplace may have multiple endpoints if desired; an event notifica"
        }
      ]
    }
  ],
  "bundled": true
}